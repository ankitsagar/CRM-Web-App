from django.conf.urls import url

# from .views import Dashboard # CustomerDetailView, CustomerSearchView

urlpatterns = [
    # url(r'^dashboard/$', Dashboard.as_view(), name='dashboard'),
    # url(r'^customer/(?P<pk>\d+)/$', CustomerDetailView.as_view(), name='customer_detail'),
    # #url(r'^customer/add/$', AddCustomer.as_view(), name='customer_add'),
    # url(r'^customer/search/$', CustomerSearchView.as_view(), name='customer_search'),

]